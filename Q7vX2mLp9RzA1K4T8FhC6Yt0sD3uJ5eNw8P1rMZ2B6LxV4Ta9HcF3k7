local a=Enum.KeyCode.E
local b=Enum.KeyCode.C
local c=false
local d=nil

local e=game:GetService("Players")
local f=game:GetService("RunService")
local g=game:GetService("UserInputService")
local h=game:GetService("StarterGui")

local i=e.LocalPlayer
local j=i:GetMouse()

local function k(l,m,n)
    h:SetCore("SendNotification",{Title=l,Text=m,Duration=n or 2})
end

k("Flick Trigger by Memphis","Trigger em execução.\n🇪 (Liga/Desliga) | 🇨 (Status)",3)

g.InputBegan:Connect(function(o,p)
    if p then return end
    if o.KeyCode==a then
        c=not c
        d=nil
    elseif o.KeyCode==b then
        k("Trigger Status",c and "Ativado" or "Desativado",2)
    end
end)

f.Heartbeat:Connect(function()
    if not c then return end
    local q=j.Target
    if q then
        local r=q.Parent
        if r and not r:FindFirstChild("Humanoid") then
            r=r.Parent
        end
        local s=r and r:FindFirstChild("Humanoid")
        if s and s.Health>0 and r.Name~=i.Name then
            if r~=d then
                d=r
                if mouse1click then mouse1click() end
            end
        else
            d=nil
        end
    else
        d=nil
    end
end)
